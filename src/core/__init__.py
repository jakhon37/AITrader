"""D01-CORE — shared foundation for all AITrader divisions.

Every other division imports from src.core.*
No domain logic lives here — contracts, bus, clock, config, ids, logging, exceptions only.
"""
